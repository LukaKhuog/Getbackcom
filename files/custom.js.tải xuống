(function($){
    var ival=[];
    var index_ival=0;
	$(document).ready(function(){		
		changeTabBrowser();			
		window_resize();
		$(window).resize(function(){
			window_resize();	
			slider_resize();
		});			
		function window_resize(){
			
		}
		function slider_resize(){
			if($(window).width()<=991){
				var perpage = 3;
				var percent = 20;
				if($(window).width()<=767){
					perpage = 2;
					if($(window).width()<=600){
						perpage = 1;
						percent = 40;
					}
				}
				slider_js($('section.banner-form-backcom .list-icon-featured>ul'),perpage,32,false,false,false,percent,0);
				slider_js($('section.banner-home .list-icon-featured>ul'),perpage,32,false,false,false,percent,0);
			}else{
				remove_slider_js($('section.banner-form-backcom .list-icon-featured>ul'));
				remove_slider_js($('section.banner-home .list-icon-featured>ul'));
			}
			if($(window).width()<=991){
				var perpage = 2;
				var margin_right = 32;
				var percent = 20;
				if($(window).width()<=767){
					perpage = 1;
					margin_right = 16;
				}
				slider_js($('section.list-steps-register .list-inner>ul'),perpage,margin_right,false,false,false,percent,0);
			}else{
				remove_slider_js($('section.list-steps-register .list-inner>ul'));
			}
			if($(window).width()<=991){
				var perpage = 2;
				var percent = 20;
				var margin_right = 32;
				if($(window).width()<=767){
					perpage = 1;
					margin_right = 16;
				}
				slider_js($('section.list-type-of-accounts .list-inner>ul'),perpage,margin_right,false,false,false,percent,0);
			}else{
				remove_slider_js($('section.list-type-of-accounts .list-inner>ul'));
			}
			if($(window).width()<=991){
				var perpage = 2;
				var percent = 20;
				var margin_right = 32;
				if($(window).width()<=600){
					perpage = 1;
					margin_right = 16;
					percent = 10;
				}
				slider_js($('section.list-why-choose-us .list-inner>ul'),perpage,margin_right,false,false,false,percent,0);
			}else{
				remove_slider_js($('section.list-why-choose-us .list-inner>ul'));
			}
			if($(window).width()<=767){
				var perpage = 2;
				var margin_right = 24;
				var percent = 18;	
				if($(window).width()<=600){
					perpage = 1;
					margin_right = 16;
				}			
				$('section.list-cat-trading-floors .list-tab-contents .item').each(function(){
					slider_js($(this).find('>ul'),perpage,margin_right,true,false,false,percent,0);
				});
			}else{
				$('section.list-cat-trading-floors .list-tab-contents .item').each(function(){
					remove_slider_js($(this).find('>ul'));
				});
			}			
			if($(window).width()<=991){
				var perpage = 2;
				var margin_right = 24;
				var percent = 18;	
				if($(window).width()<=600){
					perpage = 1;
				}
				slider_js($('section.content-single-floor-trading .list-similar-floors ul'),perpage,margin_right,false,false,false,percent,0);
			}else{
				remove_slider_js($('section.content-single-floor-trading .list-similar-floors ul'));
			}
		}
		function changeTabBrowser(){
			document.addEventListener("visibilitychange",function(){
				if(document.hidden){
					for(var i = 0;i<ival.length;i++){
						clearInterval(ival[i]);
					}
				}else{
					slider_resize();
				}
			});
		}
	});
	$('section.content-single-floor-trading .list-similar-floors').ready(function(){
		if($(window).width()<=991){
			var perpage = 2;
			var margin_right = 24;
			var percent = 18;	
			if($(window).width()<=600){
				perpage = 1;
			}
			slider_js($('section.content-single-floor-trading .list-similar-floors .slider-wrapper .slider-inner>ul'),perpage,margin_right,false,false,false,percent,0);
		}
	});
	$('section.list-cat-trading-floors .list-tab-contents').ready(function(){
		if($(window).width()<=767){
			var perpage = 2;
			var margin_right = 24;
			var percent = 18;	
			if($(window).width()<=600){
				perpage = 1;
				margin_right = 16;
			}
			$(this).find('.item').each(function(){
				slider_js($(this).find('>ul'),perpage,margin_right,true,false,false,percent,0);
			});
		}
	});
	$('section.list-why-choose-us .list-inner').ready(function(){
		if($(window).width()<=991){
			var perpage = 2;
			var percent = 20;
			var margin_right = 32;
			if($(window).width()<=600){
				perpage = 1;
				margin_right = 16;
				percent = 10;
			}
			slider_js($('section.list-why-choose-us .list-inner>ul'),perpage,margin_right,false,false,false,percent,0);
		}
	});
	$('section.list-type-of-accounts .list-inner').ready(function(){
		if($(window).width()<=991){
			var perpage = 2;
			var percent = 20;
			var margin_right = 32;
			if($(window).width()<=767){
				perpage = 1;
				margin_right = 16;
			}
			slider_js($('section.list-type-of-accounts .list-inner>ul'),perpage,margin_right,false,false,false,percent,0);
		}
	});
	$('section.list-steps-register .list-inner').ready(function(){
		if($(window).width()<=991){
			var perpage = 2;
			var margin_right = 32;
			var percent = 20;
			if($(window).width()<=767){
				perpage = 1;
				margin_right = 16;
			}
			slider_js($('section.list-steps-register .list-inner>ul'),perpage,margin_right,false,false,false,percent,0);
		}
	});
	$('section.banner-form-backcom .list-icon-featured').ready(function(){
		if($(window).width()<=991){
			var perpage = 3;
			var percent = 20;
			if($(window).width()<=767){
				perpage = 2;
				if($(window).width()<=600){
					perpage = 1;
					percent = 40;
				}
			}
			slider_js($('section.banner-form-backcom .list-icon-featured>ul'),perpage,32,false,false,false,percent,0);
		}
	});
	$('section.banner-home .list-icon-featured').ready(function(){
		if($(window).width()<=991){
			var perpage = 3;
			var percent = 20;
			if($(window).width()<=767){
				perpage = 2;
				if($(window).width()<=600){
					perpage = 1;
					percent = 40;
				}
			}
			slider_js($('section.banner-home .list-icon-featured>ul'),perpage,32,false,false,false,percent,0);
		}
	});
	function remove_slider_js($tag){	
		if($tag.length==0){
			return;
		}
        $tag.find('a').each(function(){
            if($(this).attr('href')!='' && $(this).attr('href')!='#'){
                $tag.find('a').unbind('click');
            }
        });
        $tag.unbind('mouseover');$tag.unbind('mousedown');$tag.unbind('mousemove');$tag.unbind('mouseup');$tag.unbind('mouseleave');
        $tag.unbind('touchstart');$tag.unbind('touchend');$tag.unbind('touchmove');
        $tag.removeAttr('style');
        $tag.removeAttr('data-left');
        $tag.removeAttr('data-mousedown');
        $tag.find('>li').removeAttr('style');
        $tag.parent().find('>.slider-control a').addClass('hide');
        $tag.parent().find('>.dotted_control').remove();
    }
    function slider_js($tag,items_per_page,margin_right,show_arrow,dotted_control,auto_slide,offset_percent,index_interval,time_interval = 6000,speed='normal'){
        if($tag.length==0){
			return;
		}
		$tag.css('touch-action','pan-y');
		var parent_w = $tag.parent().outerWidth();
		var offset_slider_w = (parent_w*(offset_percent/100));
		var item_w = (parent_w - ((items_per_page-1)*margin_right)-offset_slider_w)/items_per_page;
		var index_iv = $tag.attr('data-ival');
		if(index_iv=='' || typeof index_iv=='undefined'){
			index_iv = index_interval;
			$tag.attr('data-ival',index_iv);
		}else{
			index_iv = parseInt(index_iv);
		}
		var current_index = 0;
		var slider_item_w = (item_w+margin_right)*items_per_page;
		var ul_w = 0;
		var ul_h = 0;
		$tag.find('>li').removeAttr('style');
		$tag.find('>li').css('width',item_w);
		$tag.find('>li').each(function(){
			ul_w+=item_w+margin_right;
		});
		$tag.css('transition','.5s transform');
		$tag.css({'width':ul_w+items_per_page});
		setTimeout(function(){
			$tag.find('>li').each(function(){
				if(ul_h<$(this).outerHeight()){
					ul_h = $(this).outerHeight();
				}
			});
			$tag.css({'height':ul_h});
			$tag.find('>li').css('height',ul_h);
		},100);		
		if(dotted_control){
			if($tag.parent().find('.dotted_control').length>0){
				$tag.parent().find('.dotted_control').remove();
			}
			if(ul_w>parent_w+margin_right){
				var items_num = Math.ceil($tag.find('>li').length/items_per_page);			
				var tag_dotted = '<div class="dotted_control"><ul>';
				for(var i=0;i<items_num;i++){
					tag_dotted+='<li></li>';
				}
				tag_dotted+='</ul></div>';
				$tag.parent().append($(tag_dotted));
				$tag.parent().find('.dotted_control').find('ul li').unbind('click');
				$tag.parent().find('.dotted_control').find('ul li').click(function(e){
					var index = $(this).index();
					if(!$(this).hasClass('active')){
						var old_active = $(this).parent().find('li.active').index();
						$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
						$(this).addClass('active');
						var current_active = $(this).index();
						var data_left = -index*(slider_item_w);
						if(data_left < -(ul_w - (parent_w+margin_right))){
							data_left = -(ul_w - (parent_w+margin_right));
						}
						$tag.attr('data-left',data_left);
						$tag.attr('data-moving',1);
						if(Math.abs(old_active-current_active)>=3){
							$tag.css('transition','1s transform');
						}
						$tag.css({'transform':'translateX('+data_left+'px)'});
						if(Math.abs(old_active-current_active)>=3){
							setTimeout(function(){
								$tag.attr('data-moving','');
								$tag.css('transition','.5s transform');
							},1000);

						}else{
							setTimeout(function(){
								$tag.attr('data-moving','');
							},500);
						}
						if(show_arrow){
							if(data_left==0){
								$tag.parent().find('.slider-control a.prev').addClass('hide');	
								$tag.parent().find('.slider-control a.next').removeClass('hide');							
							}else{
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}
							if(data_left==-(ul_w-(parent_w+margin_right))){
								$tag.parent().find('.slider-control a.next').addClass('hide');
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}else{
								$tag.parent().find('.slider-control a.next').removeClass('hide');							
							} 
						}
						if(auto_slide){
							clearInterval(ival[index_iv]);
							ival[index_iv] = setInterval(function(){
								data_left = $tag.attr('data-left');
								if(typeof data_left == 'undefined' || data_left == ''){
									data_left = 0;
								}else{
									data_left = parseFloat(data_left);
								}
								if(data_left>-(ul_w-(parent_w+margin_right)) && data_left-(item_w+margin_right)>-(ul_w-(parent_w+margin_right))){
									data_left-=item_w+margin_right;
								}else{
									if(data_left>-(ul_w-(parent_w+margin_right))){
										data_left = -(ul_w-(parent_w+margin_right));
									}else{
										data_left-=item_w+margin_right;
									}
								}
								if(data_left<-(ul_w-(parent_w+margin_right))){
									data_left = 0;
									$tag.addClass('return-start');
									$tag.css('transition','1s transform');
								}
								$tag.attr('data-moving',1);
								$tag.attr('data-left',data_left); 
								$tag.css({'transform':'translateX('+data_left+'px)'});
								if($tag.hasClass('return-start')){
									$tag.removeClass('return-start');
									setTimeout(function(){
										$tag.attr('data-moving','');
										$tag.css('transition','.5s transform');
									},1000);
								}else{
									setTimeout(function(){
										$tag.attr('data-moving','');
									},500);									
								}
								if(show_arrow){									
									if(data_left==0){
										$tag.parent().find('.slider-control a.prev').addClass('hide');	
										$tag.parent().find('.slider-control a.next').removeClass('hide');							
									}else{
										$tag.parent().find('.slider-control a.prev').removeClass('hide');
									}
									if(data_left==-(ul_w-(parent_w+margin_right))){
										$tag.parent().find('.slider-control a.next').addClass('hide');
										$tag.parent().find('.slider-control a.prev').removeClass('hide');
									}else{
										$tag.parent().find('.slider-control a.next').removeClass('hide');							
									} 
								}
								if(dotted_control){
									index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
									var index_page = index_active;
									index_page = Math.floor(index_page/items_per_page);
									if(index_active==$tag.find('>li').length-items_per_page){
										index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
									}
									$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
									$tag.parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
								}
							},time_interval);
						}   
					}
				});
			}else{
				$tag.parent().find('.slider-control a').addClass('hide');
			}
		}
		var index_active = 0;
		var data_left = $tag.attr('data-left');
		if(typeof data_left!='undefined' && data_left!=''){
			data_left = parseFloat(data_left);
		}else{
			data_left = 0;
		}
		index_active = Math.round(Math.abs(data_left)/(item_w+margin_right));
		data_left = -index_active*(item_w+margin_right);
		if(data_left>0){
			data_left = 0;
		}
		if(data_left<-(ul_w-(parent_w+margin_right))){
			data_left=-(ul_w-(parent_w+margin_right));
		}
		if(ul_w<parent_w+margin_right){
			data_left = 0;
		}
		if(dotted_control){
			index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
			var index_page = index_active;
			index_page = Math.floor(index_page/items_per_page);
			if(index_active==$tag.find('>li').length-items_per_page){
				index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
			}
			$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
			$tag.parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
		}
		$tag.css({'transform':'translateX('+data_left+'px)'});
		$tag.attr('data-left',data_left); 
		$tag.css('visibility','visible');
		$tag.parent().removeClass('preload-slider');		
		var total_page = Math.ceil($tag.find('>li').length/items_per_page);
		var current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
		if(data_left==-(ul_w-(parent_w+margin_right))){
			current_page = total_page;
		}
		if($tag.parent().find('.slider-index-page').length>0){
			var tag_counter = 'Trang <span>';
			if(current_page<10){
				tag_counter+='0'+current_page;
			}else{
				tag_counter+current_page;
			}
			tag_counter+='</span> / ';
			if(total_page<10){
				tag_counter+='0'+total_page;
			}else{
				tag_counter+=total_page;
			}
			$tag.parent().find('.slider-index-page').html(tag_counter);
		}
		$tag.unbind('mousedown');$tag.unbind('mouseover');$tag.unbind('mousemove');$tag.unbind('mouseup');$tag.unbind('mouseleave');
		if(ul_w>parent_w+margin_right){
			if(data_left==0){
				$tag.parent().find('.slider-control a.prev').addClass('hide');
				$tag.parent().find('.slider-control a.next').removeClass('hide');
			}else{
				$tag.parent().find('.slider-control a.prev').removeClass('hide');
			}
			if(data_left==-(ul_w-(parent_w+margin_right))){
				$tag.parent().find('.slider-control a.next').addClass('hide');
				$tag.parent().find('.slider-control a.prev').removeClass('hide');
			}else{
				$tag.parent().find('.slider-control a.next').removeClass('hide');
			}
			$tag.find('li a').unbind('click');
			$tag.find('li a').click(function(e){e.preventDefault();});  
			$tag.mousedown(function(e){
				e.preventDefault();
				if(e.which==1){
					var data_moving = $(this).attr('data-moving');
					if(data_moving=='' || typeof data_moving=='undefined'){
						$tag.attr('data-mousedown',e.pageX);		
					}
				}
			});		
			$tag.mouseover(function(e){
				if(auto_slide){
					clearInterval(ival[index_iv]);
				}
			});
			$tag.mousemove(function(e){
				var data_mousedown = $(this).attr('data-mousedown');
				if(typeof data_mousedown!='undefined' && data_mousedown!=''){
					var data_moving = $(this).attr('data-moving');
					if(data_moving=='' || typeof data_moving=='undefined'){
						data_mousedown = parseInt(data_mousedown);
						$(this).css('transition','');
						var data_left = $(this).attr('data-left');
						if(typeof data_left!='undefined' && data_left!=''){
							data_left = parseFloat(data_left);
						}else{
							data_left = 0;
						}
						data_left+=e.pageX-data_mousedown;
						if(data_left>slider_item_w/10){
							data_left = slider_item_w/10;
						}
						if(ul_w>=slider_item_w){
							if(data_left<-(ul_w-0.9*(slider_item_w))){
								data_left=-(ul_w-0.9*(slider_item_w));
							}
						}else{
							if(data_left<-(ul_w-0.9*(slider_item_w))){
								data_left=-(ul_w-0.9*(slider_item_w));
							}
						}
						if(ul_w>parent_w+margin_right){
							$(this).css({'transform':'translateX('+data_left+'px)'});
						}
					}
				}
			});
			$tag.mouseup(function(e){
				e.preventDefault();
				var data_mousedown = $(this).attr('data-mousedown');
				if(typeof data_mousedown!='undefined' && data_mousedown!=''){
					var data_moving = $(this).attr('data-moving');
					if(data_moving=='' || typeof data_moving=='undefined'){
						data_mousedown = parseInt(data_mousedown);
						$(this).css('transition','transform .5s');
						var data_left = $(this).attr('data-left');
						if(typeof data_left!='undefined' && data_left!=''){
							data_left = parseFloat(data_left);
						}else{
							data_left = 0;
						}
						var swipe_w = Math.abs(e.pageX-data_mousedown);
						if(swipe_w>10){
							if(swipe_w%(item_w+margin_right)>=(item_w+margin_right)/10){
								swipe_w = Math.ceil(swipe_w/(item_w+margin_right))*(item_w+margin_right);
							}else{
								swipe_w = Math.floor(swipe_w/(item_w+margin_right))*(item_w+margin_right);
							}	
							if(e.pageX<data_mousedown){
								swipe_w=-swipe_w;
							}
							if(data_left==-(ul_w-(parent_w+margin_right))){
								if(swipe_w>0){
									if(Math.ceil($tag.find('>li').length/items_per_page)>2){
										data_left = -(Math.ceil($tag.find('>li').length/items_per_page)-1)*(slider_item_w)+(item_w+margin_right);
									}else{
										data_left = -($tag.find('>li').length-items_per_page-1)*(item_w+margin_right);
									}
								}else{
									data_left+=swipe_w;
								}
							}else{
								data_left+=swipe_w;
							}							
							if(data_left<0 && data_left>-(ul_w-(parent_w+margin_right))){
								index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
								data_left = -index_active*(item_w+margin_right);
							}
							if(data_left>0){data_left = 0;}
							if(data_left<-(ul_w-(parent_w+margin_right))){data_left=-(ul_w-(parent_w+margin_right));}
						}else{
							if($tag.find('>li a').length>0){
								var $anchor = $tag.find('>li a');
								var $target = $(e.target);
								if(Math.abs(e.pageX-data_mousedown)<10){
									$anchor.each(function(){
										if($(this).attr('href')!='' && $(this).attr('href')!='#'){
											if(e.pageX>=$(this).offset().left && e.pageX<=$(this).offset().left+$(this).outerWidth() && e.pageY>=$(this).offset().top && e.pageY<=$(this).offset().top+$(this).outerHeight()){
												if($target.is($(this)) || $target.is($(this).find('*'))){
													if(typeof $(this).attr('href')!='undefined'){
														if(!$(this).parent().is($('.yith-wcwl-add-button'))){
															if($(this).attr('target')=='_blank'){
																window.open($(this).attr('href'),'_blank');
															}else{
																location.href = $(this).attr('href');
															}
														}
													}
												}
											}
										}
									});
								}
							}	
						}	
						if($tag.parent().find('.slider-index-page').length>0){
							current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
							if(data_left==-(ul_w-(parent_w+margin_right))){
								current_page = total_page;
							}
							var tag = '';
							if(current_page<10){
								tag+='0'+current_page;
							}else{
								tag+=current_page;
							}	
							$tag.parent().find('.slider-index-page span').html(tag);
						}
						if(dotted_control){
							index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
							var index_page = index_active;
							index_page = Math.floor(index_page/items_per_page);
							if(index_active==$tag.find('>li').length-items_per_page){
								index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
							}
							$(this).parent().find('.dotted_control').find('ul li').removeClass('active');
							$(this).parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
						}
						if(ul_w>parent_w+margin_right){
							$(this).attr('data-moving',1);
							$(this).css({'transform':'translateX('+data_left+'px)'});
							setTimeout(function(){
								$tag.attr('data-moving','');
							},500);
							$(this).attr('data-left',data_left);
							if(data_left==0){
								$tag.parent().find('.slider-control a.prev').addClass('hide');
								$tag.parent().find('.slider-control a.next').removeClass('hide');
							}else{
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}
							if(data_left==-(ul_w-(parent_w+margin_right))){
								$tag.parent().find('.slider-control a.next').addClass('hide');
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}else{
								$tag.parent().find('.slider-control a.next').removeClass('hide');
							}
						}
						$(this).attr('data-mousedown','');
					}
				}
			});
			$tag.mouseleave(function(e){
				var data_mousedown = $(this).attr('data-mousedown');
				if(typeof data_mousedown!='undefined' && data_mousedown!=''){
					var data_moving = $(this).attr('data-moving');
					if(data_moving=='' || typeof data_moving=='undefined'){
						data_mousedown = parseInt(data_mousedown);
						$(this).css('transition','transform .5s');
						var data_left = $(this).attr('data-left');
						if(typeof data_left!='undefined' && data_left!=''){
							data_left = parseFloat(data_left);
						}else{
							data_left = 0;
						}
						var swipe_w = Math.abs(e.pageX-data_mousedown);
						if(swipe_w>10){
							if(swipe_w%(item_w+margin_right)>=(item_w+margin_right)/10){
								swipe_w = Math.ceil(swipe_w/(item_w+margin_right))*(item_w+margin_right);
							}else{
								swipe_w = Math.floor(swipe_w/(item_w+margin_right))*(item_w+margin_right);
							}	
							if(e.pageX<data_mousedown){
								swipe_w=-swipe_w;
							}							
							if(data_left==-(ul_w-(parent_w+margin_right))){
								if(swipe_w>0){
									if(Math.ceil($tag.find('>li').length/items_per_page)>2){
										data_left = -(Math.ceil($tag.find('>li').length/items_per_page)-1)*(slider_item_w)+(item_w+margin_right);
									}else{
										data_left = -($tag.find('>li').length-items_per_page-1)*(item_w+margin_right);
									}
								}else{
									data_left+=swipe_w;
								}
							}else{
								data_left+=swipe_w;
							}							
							if(data_left<0 && data_left>-(ul_w-(parent_w+margin_right))){
								index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
								data_left = -index_active*(item_w+margin_right);
							}
							if(data_left>0){data_left = 0;}
							if(data_left<-(ul_w-(parent_w+margin_right))){data_left=-(ul_w-(parent_w+margin_right));}
						}
						if($tag.parent().find('.slider-index-page').length>0){
							current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
							if(data_left==-(ul_w-(parent_w+margin_right))){
								current_page = total_page;
							}
							var tag = '';
							if(current_page<10){
								tag+='0'+current_page;
							}else{
								tag+=current_page;
							}	
							$tag.parent().find('.slider-index-page span').html(tag);
						}
						if(dotted_control){
							index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
							var index_page = index_active;
							index_page = Math.floor(index_page/items_per_page);
							if(index_active==$tag.find('>li').length-items_per_page){
								index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
							}
							$(this).parent().find('.dotted_control').find('ul li').removeClass('active');
							$(this).parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
						}
						if(ul_w>parent_w+margin_right){
							$(this).attr('data-moving',1);
							$(this).css({'transform':'translateX('+data_left+'px)'});
							setTimeout(function(){
								$tag.attr('data-moving','');
							},500);
							$(this).attr('data-left',data_left);
							if(data_left==0){
								$tag.parent().find('.slider-control a.prev').addClass('hide');
								$tag.parent().find('.slider-control a.next').removeClass('hide');
							}else{
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}
							if(data_left==-(ul_w-(parent_w+margin_right))){
								$tag.parent().find('.slider-control a.next').addClass('hide');
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}else{
								$tag.parent().find('.slider-control a.next').removeClass('hide');
							}
						}
						$(this).attr('data-mousedown','');
					}
				}
				if(auto_slide){
					clearInterval(ival[index_iv]);
					ival[index_iv] = setInterval(function(){
						data_left = $tag.attr('data-left');
						if(typeof data_left == 'undefined' || data_left == ''){
							data_left = 0;
						}else{
							data_left = parseFloat(data_left);
						}
						if(data_left>-(ul_w-(parent_w+margin_right)) && data_left-(item_w+margin_right)>-(ul_w-(parent_w+margin_right))){
							data_left-=item_w+margin_right;
						}else{
							if(data_left>-(ul_w-(parent_w+margin_right))){
								data_left = -(ul_w-(parent_w+margin_right));
							}else{
								data_left-=item_w+margin_right;
							}
						}
						if(data_left<-(ul_w-(parent_w+margin_right))-1){
							data_left = 0;
							$tag.addClass('return-start');
							$tag.css('transition','1s transform');
						}
						$tag.attr('data-moving',1);
						$tag.attr('data-left',data_left); 
						$tag.css({'transform':'translateX('+data_left+'px)'});
						if($tag.hasClass('return-start')){
							$tag.removeClass('return-start');
							setTimeout(function(){
								$tag.attr('data-moving','');
								$tag.css('transition','.5s transform');
							},1000);
						}else{
							setTimeout(function(){
								$tag.attr('data-moving','');
							},500);									
						}
						if(show_arrow){									
							if(data_left==0){
								$tag.parent().find('.slider-control a.prev').addClass('hide');	
								$tag.parent().find('.slider-control a.next').removeClass('hide');							
							}else{
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}
							if(data_left==-(ul_w-(parent_w+margin_right))){
								$tag.parent().find('.slider-control a.next').addClass('hide');
								$tag.parent().find('.slider-control a.prev').removeClass('hide');
							}else{
								$tag.parent().find('.slider-control a.next').removeClass('hide');							
							} 
						}
						if(dotted_control){
							index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
							var index_page = index_active;
							index_page = Math.floor(index_page/items_per_page);
							if(index_active==$tag.find('>li').length-items_per_page){
								index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
							}
							$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
							$tag.parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
						}
						if($tag.parent().find('.slider-index-page').length>0){
							current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
							if(data_left==-(ul_w-(parent_w+margin_right))){
								current_page = total_page;
							}
							var tag = '';
							if(current_page<10){
								tag+='0'+current_page;
							}else{
								tag+=current_page;
							}	
							$tag.parent().find('.slider-index-page span').html(tag);
						}
					},time_interval);
				} 
			});
		}
		$tag.unbind('touchstart');
		$tag.unbind('touchmove');
		$tag.unbind('touchend');
		if(ul_w>parent_w+margin_right){
			$tag.on('touchstart',function(e){
				var data_moving = $(this).attr('data-moving');
				if(data_moving=='' || typeof data_moving=='undefined'){
					$(this).attr('data-mousedown',e.originalEvent.touches[0].pageX);
					$(this).attr('data-mousedowny',e.originalEvent.touches[0].pageY);
					if(auto_slide){
						clearInterval(ival[index_iv]);
					}
				}				
			});
			$tag.on('touchmove',function(e){
				var data_moving = $(this).attr('data-moving');
				if(data_moving=='' || typeof data_moving=='undefined'){
					var data_scroll = $(this).attr('data-scroll');
					var data_mousedown = $(this).attr('data-mousedown');
					if(typeof data_mousedown!='undefined' && data_mousedown!=''){
						data_mousedown = parseInt(data_mousedown);
						$(this).css('transition','');
						var data_mousedowny = $(this).attr('data-mousedowny');
						data_mousedowny = parseInt(data_mousedowny);
						if(Math.abs(e.originalEvent.touches[0].pageX-data_mousedown)>10 || Math.abs(e.originalEvent.touches[0].pageY-data_mousedowny)){
							if(Math.abs(e.originalEvent.touches[0].pageX-data_mousedown)>Math.abs(e.originalEvent.touches[0].pageY-data_mousedowny)){
								if(data_scroll!='1' || typeof data_scroll=='undefined'){
									$(this).attr('data-scroll','2');
									var data_left = $(this).attr('data-left');
									if(typeof data_left!='undefined' && data_left!=''){
										data_left = parseFloat(data_left);
									}else{
										data_left = 0;
									}
									data_left+=e.originalEvent.touches[0].pageX-data_mousedown;
									if(data_left>slider_item_w/10){
										data_left = slider_item_w/10;
									}
									if(data_left<-(ul_w-0.9*(slider_item_w))){
										data_left=-(ul_w-0.9*(slider_item_w));
									}					
									if(ul_w>parent_w){
										$(this).css({'transform':'translateX('+data_left+'px)'});
									}
								}
							}else{
								if(data_scroll!='2' || typeof data_scroll=='undefined'){
									$(this).attr('data-scroll','1');
								}else{
									if(data_scroll!='1' || typeof data_scroll=='undefined'){
										$(this).attr('data-scroll','2');
										if(Math.abs(e.originalEvent.touches[0].pageX-data_mousedown)>0){
											var data_left = $(this).attr('data-left');
											if(typeof data_left!='undefined' && data_left!=''){
												data_left = parseFloat(data_left);
											}else{
												data_left = 0;
											}
											data_left+=e.originalEvent.touches[0].pageX-data_mousedown;
											if(data_left>slider_item_w/10){
												data_left = slider_item_w/10;
											}
											if(data_left<-(ul_w-0.9*(slider_item_w))){
												data_left=-(ul_w-0.9*(slider_item_w));
											}		
											if(ul_w>parent_w){
												$(this).css({'transform':'translateX('+data_left+'px)'});
											}
										}
									}
								}    
							}
						}
					}                
				}
			});
			$tag.on('touchend',function(e){ 
				var data_moving = $(this).attr('data-moving');
				if(data_moving=='' || typeof data_moving=='undefined'){               
					var data_scroll = $(this).attr('data-scroll');
					if(data_scroll=='2' || typeof data_scroll=='undefined'){
						var data_mousedown = $(this).attr('data-mousedown');
						if(typeof data_mousedown!='undefined' && data_mousedown!=''){
							data_mousedown = parseInt(data_mousedown);
							$(this).css('transition','transform .5s');
							var data_left = $(this).attr('data-left');
							if(typeof data_left!='undefined' && data_left!=''){
								data_left = parseFloat(data_left);
							}else{
								data_left = 0;
							}
							var swipe_w = Math.abs(e.originalEvent.changedTouches[0].pageX-data_mousedown);
							if(swipe_w>10){
								if(swipe_w%(item_w+margin_right)>=(item_w+margin_right)/10){
									swipe_w = Math.ceil(swipe_w/(item_w+margin_right))*(item_w+margin_right);
								}else{
									swipe_w = Math.floor(swipe_w/(item_w+margin_right))*(item_w+margin_right);
								}	
								if(e.originalEvent.changedTouches[0].pageX<data_mousedown){
									swipe_w=-swipe_w;
								}								
								if(data_left==-(ul_w-(parent_w+margin_right))){
									if(swipe_w>0){
										if(Math.ceil($tag.find('>li').length/items_per_page)>2){
											data_left = -(Math.ceil($tag.find('>li').length/items_per_page)-1)*(slider_item_w)+(item_w+margin_right);
										}else{
											data_left = -($tag.find('>li').length-items_per_page-1)*(item_w+margin_right);
										}
									}else{
										data_left+=swipe_w;
									}
								}else{
									data_left+=swipe_w;
								}		
								if(data_left<0 && data_left>-(ul_w-(parent_w+margin_right))){
									index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
									data_left = -index_active*(item_w+margin_right);
								}								
								if(data_left>0){data_left = 0;}
								if(data_left<-(ul_w-(parent_w+margin_right))){data_left=-(ul_w-(parent_w+margin_right));}
							}					
							if(dotted_control){
								index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
								var index_page = index_active;
								index_page = Math.floor(index_page/items_per_page);
								if(index_active==$tag.find('>li').length-items_per_page){
									index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
								}
								$(this).parent().find('.dotted_control').find('ul li').removeClass('active');
								$(this).parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
							}							
							if(show_arrow){									
								if(data_left==0){
									$tag.parent().find('.slider-control a.prev').addClass('hide');	
									$tag.parent().find('.slider-control a.next').removeClass('hide');							
								}else{
									$tag.parent().find('.slider-control a.prev').removeClass('hide');
								}
								if(data_left==-(ul_w-(parent_w+margin_right))){
									$tag.parent().find('.slider-control a.next').addClass('hide');
									$tag.parent().find('.slider-control a.prev').removeClass('hide');
								}else{
									$tag.parent().find('.slider-control a.next').removeClass('hide');							
								} 
							}
							if($tag.parent().find('.slider-index-page').length>0){
								current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
								if(data_left==-(ul_w-(parent_w+margin_right))){
									current_page = total_page;
								}
								var tag = '';
								if(current_page<10){
									tag+='0'+current_page;
								}else{
									tag+=current_page;
								}	
								$tag.parent().find('.slider-index-page span').html(tag);
							}
							if(ul_w>parent_w+margin_right){
								$(this).attr('data-moving',1);
								$(this).css({'transform':'translateX('+data_left+'px)'});
								setTimeout(function(){
									$tag.attr('data-moving','');
								},500);
								$(this).attr('data-left',data_left);
							}
							$(this).attr('data-mousedown',''); 
							$(this).attr('data-mousedowny','');
							$(this).attr('data-scroll','');                   
						}
					}else{
						$(this).attr('data-mousedown','');
						$(this).attr('data-mousedowny','');
						$(this).attr('data-scroll','');
					}
					if(auto_slide){
						clearInterval(ival[index_iv]);
						ival[index_iv] = setInterval(function(){
							data_left = $tag.attr('data-left');
							if(typeof data_left == 'undefined' || data_left == ''){
								data_left = 0;
							}else{
								data_left = parseFloat(data_left);
							}
							if(data_left>-(ul_w-(parent_w+margin_right)) && data_left-(item_w+margin_right)>-(ul_w-(parent_w+margin_right))){
								data_left-=item_w+margin_right;
							}else{
								if(data_left>-(ul_w-(parent_w+margin_right))){
									data_left = -(ul_w-(parent_w+margin_right));
								}else{
									data_left-=item_w+margin_right;
								}
							}
							if(data_left<-(ul_w-(parent_w+margin_right))-1){
								data_left = 0;
								$tag.addClass('return-start');
								$tag.css('transition','1s transform');
							}
							$tag.attr('data-moving',1);
							$tag.attr('data-left',data_left); 
							$tag.css({'transform':'translateX('+data_left+'px)'});
							if($tag.hasClass('return-start')){
								$tag.removeClass('return-start');
								setTimeout(function(){
									$tag.attr('data-moving','');
									$tag.css('transition','.5s transform');
								},1000);
							}else{
								setTimeout(function(){
									$tag.attr('data-moving','');
								},500);									
							}
							if(show_arrow){									
								if(data_left==0){
									$tag.parent().find('.slider-control a.prev').addClass('hide');	
									$tag.parent().find('.slider-control a.next').removeClass('hide');							
								}else{
									$tag.parent().find('.slider-control a.prev').removeClass('hide');
								}
								if(data_left==-(ul_w-(parent_w+margin_right))){
									$tag.parent().find('.slider-control a.next').addClass('hide');
									$tag.parent().find('.slider-control a.prev').removeClass('hide');
								}else{
									$tag.parent().find('.slider-control a.next').removeClass('hide');							
								} 
							}
							if(dotted_control){
								index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
								var index_page = index_active;
								index_page = Math.floor(index_page/items_per_page);
								if(index_active==$tag.find('>li').length-items_per_page){
									index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
								}
								$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
								$tag.parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
							}
							if($tag.parent().find('.slider-index-page').length>0){
								current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
								if(data_left==-(ul_w-(parent_w+margin_right))){
									current_page = total_page;
								}
								var tag = '';
								if(current_page<10){
									tag+='0'+current_page;
								}else{
									tag+=current_page;
								}	
								$tag.parent().find('.slider-index-page span').html(tag);
							}
						},time_interval);
					}
				}				
			});
		}
		if(show_arrow){
			if($tag.parent().find('div.slider-control').length==0 && ul_w>parent_w+margin_right){
				$tag.parent().append($('<div class="slider-control"><a href="#" class="prev hide"></a><a href="#" class="next"></a></div>'));
			}
			if(ul_w<=parent_w+margin_right){$tag.parent().find('.slider-control a').addClass('hide');}
			$tag.parent().find('.slider-control a').unbind('click');
			$tag.parent().find('.slider-control a').click(function(e){
				e.preventDefault();
				var data_moving = $(this).attr('data-moving');
				if(data_moving=='' || typeof data_moving=='undefined'){   
					var data_left = $tag.attr('data-left');
					if(typeof data_left!='undefined' && data_left!=''){
						data_left = parseFloat(data_left);
					}else{
						data_left = 0;
					}
					if($(this).hasClass('prev')){
						data_left+=item_w+margin_right;
						if(data_left<=0){
							index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
							data_left = -index_active*(item_w+margin_right);
						}
					}
					if($(this).hasClass('next')){
						if(data_left>-(ul_w-(parent_w+margin_right)) && data_left-(item_w+margin_right)>-(ul_w-(parent_w+margin_right))){
							data_left-=item_w+margin_right;
						}else{
							if(data_left>-(ul_w-(parent_w+margin_right))){
								data_left = -(ul_w-(parent_w+margin_right));
							}else{
								data_left-=item_w+margin_right;
							}
						}
					}
					if(data_left>0){
						data_left = -(ul_w - (parent_w+margin_right));
						$tag.addClass('return-start');
						$tag.css('transition','1s transform');
						$(this).parent().find('a.next').addClass('hide');
					}else{
						$(this).parent().find('a.next').removeClass('hide');
					}
					if(data_left<-(ul_w-(parent_w+margin_right))){
						data_left = 0;
						$tag.addClass('return-start');
						$tag.css('transition','1s transform');
						$(this).parent().find('a.prev').addClass('hide');
					}else{
						$(this).parent().find('a.prev').removeClass('hide');
					}
					if(dotted_control){
						index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
						var index_page = index_active;
						index_page = Math.floor(index_page/items_per_page);
						if(index_active==$tag.find('>li').length-items_per_page){
							index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
						}
						$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
						$tag.parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
					}										
					if(show_arrow){									
						if(data_left==0){
							$tag.parent().find('.slider-control a.prev').addClass('hide');	
							$tag.parent().find('.slider-control a.next').removeClass('hide');							
						}else{
							$tag.parent().find('.slider-control a.prev').removeClass('hide');
						}
						if(data_left==-(ul_w-(parent_w+margin_right))){
							$tag.parent().find('.slider-control a.next').addClass('hide');
							$tag.parent().find('.slider-control a.prev').removeClass('hide');
						}else{
							$tag.parent().find('.slider-control a.next').removeClass('hide');							
						} 
					}
					$tag.attr('data-moving',1);
					$tag.css({'transform':'translateX('+data_left+'px)'});				
					if($tag.hasClass('return-start')){
						$tag.removeClass('return-start');
						setTimeout(function(){
							$tag.attr('data-moving','');
							$tag.css('transition','.5s transform');
						},1000);
					}else{
						setTimeout(function(){
							$tag.attr('data-moving','');
						},500);									
					}
					$tag.attr('data-left',data_left);
					if($tag.parent().find('.slider-index-page').length>0){
						current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
						if(data_left==-(ul_w-(parent_w+margin_right))){
							current_page = total_page;
						}
						var tag = '';
						if(current_page<10){
							tag+='0'+current_page;
						}else{
							tag+=current_page;
						}	
						$tag.parent().find('.slider-index-page span').html(tag);
					}					
					if(auto_slide){
						clearInterval(ival[index_iv]);
						ival[index_iv] = setInterval(function(){
							data_left = $tag.attr('data-left');
							if(typeof data_left == 'undefined' || data_left == ''){
								data_left = 0;
							}else{
								data_left = parseFloat(data_left);
							}
							if(data_left>-(ul_w-(parent_w+margin_right)) && data_left-(item_w+margin_right)>-(ul_w-(parent_w+margin_right))){
								data_left-=item_w+margin_right;
							}else{
								if(data_left>-(ul_w-(parent_w+margin_right))){
									data_left = -(ul_w-(parent_w+margin_right));
								}else{
									data_left-=item_w+margin_right;
								}
							}
							if(data_left<-(ul_w-(parent_w+margin_right))-1){
								data_left = 0;
								$tag.addClass('return-start');
								$tag.css('transition','1s transform');
							}
							$tag.attr('data-moving',1);
							$tag.attr('data-left',data_left); 
							$tag.css({'transform':'translateX('+data_left+'px)'});
							if($tag.hasClass('return-start')){
								$tag.removeClass('return-start');
								setTimeout(function(){
									$tag.attr('data-moving','');
									$tag.css('transition','.5s transform');
								},1000);
							}else{
								setTimeout(function(){
									$tag.attr('data-moving','');
								},500);									
							}
							if(show_arrow){									
								if(data_left==0){
									$tag.parent().find('.slider-control a.prev').addClass('hide');	
									$tag.parent().find('.slider-control a.next').removeClass('hide');							
								}else{
									$tag.parent().find('.slider-control a.prev').removeClass('hide');
								}
								if(data_left==-(ul_w-(parent_w+margin_right))){
									$tag.parent().find('.slider-control a.next').addClass('hide');
									$tag.parent().find('.slider-control a.prev').removeClass('hide');
								}else{
									$tag.parent().find('.slider-control a.next').removeClass('hide');							
								} 
							}
							if(dotted_control){
								index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
								var index_page = index_active;
								index_page = Math.floor(index_page/items_per_page);
								if(index_active==$tag.find('>li').length-items_per_page){
									index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
								}
								$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
								$tag.parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
							}
							if($tag.parent().find('.slider-index-page').length>0){
								current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
								if(data_left==-(ul_w-(parent_w+margin_right))){
									current_page = total_page;
								}								
								var tag = '';
								if(current_page<10){
									tag+='0'+current_page;
								}else{
									tag+=current_page;
								}	
								$tag.parent().find('.slider-index-page span').html(tag);
							}
						},time_interval);
					}     
				}				
			});
		}    
		if(auto_slide){
			clearInterval(ival[index_iv]);
			ival[index_iv] = setInterval(function(){
				data_left = $tag.attr('data-left');
				if(typeof data_left == 'undefined' || data_left == ''){
					data_left = 0;
				}else{
					data_left = parseFloat(data_left);
				}
				if(data_left>-(ul_w-(parent_w+margin_right)) && data_left-(item_w+margin_right)>-(ul_w-(parent_w+margin_right))){
					data_left-=item_w+margin_right;
				}else{
					if(data_left>-(ul_w-(parent_w+margin_right))){
						data_left = -(ul_w-(parent_w+margin_right));
					}else{
						data_left-=item_w+margin_right;
					}
				}
				if(data_left<-(ul_w-(parent_w+margin_right))-1){
					data_left = 0;
					$tag.addClass('return-start');
					$tag.css('transition','1s transform');
				}
				$tag.attr('data-moving',1);
				$tag.attr('data-left',data_left); 
				$tag.css({'transform':'translateX('+data_left+'px)'});
				if($tag.hasClass('return-start')){
					$tag.removeClass('return-start');
					setTimeout(function(){
						$tag.attr('data-moving','');
						$tag.css('transition','.5s transform');
					},1000);
				}else{
					setTimeout(function(){
						$tag.attr('data-moving','');
					},500);									
				}
				if(show_arrow){									
					if(data_left==0){
						$tag.parent().find('.slider-control a.prev').addClass('hide');	
						$tag.parent().find('.slider-control a.next').removeClass('hide');							
					}else{
						$tag.parent().find('.slider-control a.prev').removeClass('hide');
					}
					if(data_left==-(ul_w-(parent_w+margin_right))){
						$tag.parent().find('.slider-control a.next').addClass('hide');
						$tag.parent().find('.slider-control a.prev').removeClass('hide');
					}else{
						$tag.parent().find('.slider-control a.next').removeClass('hide');							
					} 
				}
				if(dotted_control){
					index_active = Math.round(Math.abs(data_left/(item_w+margin_right)));
					var index_page = index_active;
					index_page = Math.floor(index_page/items_per_page);
					if(index_active==$tag.find('>li').length-items_per_page){
						index_page = $tag.parent().find('.dotted_control').find('ul li').length-1;
					}
					$tag.parent().find('.dotted_control').find('ul li').removeClass('active');
					$tag.parent().find('.dotted_control').find('ul li:nth-child('+(index_page+1)+')').addClass('active');
				}
				if($tag.parent().find('.slider-index-page').length>0){
					current_page = Math.floor(Math.abs(data_left)/slider_item_w)+1;
					if(data_left==-(ul_w-(parent_w+margin_right))){
						current_page = total_page;
					}
					var tag = '';
					if(current_page<10){
						tag+='0'+current_page;
					}else{
						tag+=current_page;
					}	
					$tag.parent().find('.slider-index-page span').html(tag);
				}
			},time_interval);
		}         
    }
})(jQuery)